- hosts: all
  roles: 
    - role: utils
      # add a custom package
      utils_packages:
        - ruby
  post_tasks:
    - name: check vim
      file:
        path: /usr/bin/vim
        state: file
    - name: check ruby
      file:
        path: /usr/bin/ruby
        state: file
